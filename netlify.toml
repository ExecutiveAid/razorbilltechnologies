[build]
  command = "cd razorbill-nextjs && npm install && npm run build"
  publish = "razorbill-nextjs/out"
  base = "/"

[build.environment]
  NODE_VERSION = "18"

# Next.js static export - handle client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false 